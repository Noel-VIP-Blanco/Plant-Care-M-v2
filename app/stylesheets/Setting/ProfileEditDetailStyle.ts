import { StyleSheet } from "react-native";
export const ProfileEditDetailStyle = StyleSheet.create({
  bottomButtonContainer: {
    flexDirection: "row",
    alignItems: "center",
    marginTop: 2,
  },
  textInputStyle: { margin: 7 },
});
