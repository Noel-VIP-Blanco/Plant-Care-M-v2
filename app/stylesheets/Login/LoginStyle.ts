import { StyleSheet } from "react-native";

export const LoginStyle = StyleSheet.create({
  titleText: {
    fontSize: 80,
    fontWeight: "bold",
    color: "#36d436",
    textAlign: "center",
  },
  titleSubText: {
    textAlign: "center",
    fontSize: 30,
    fontWeight: "bold",
    marginBottom: 30,
  },

  logInButton: {
    backgroundColor: "#60b147",
    marginTop: 15,
    marginBottom: -25,
  },
});
